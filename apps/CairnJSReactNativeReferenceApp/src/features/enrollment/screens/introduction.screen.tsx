import React from 'react';
import {Screen} from '@cairnjs/react-native';
import {View, Text} from 'react-native';

export const IntroductionScreen = Screen(() => {
  return (
    <View>
      <Text>We'll walk you through signing up</Text>
    </View>
  );
});
